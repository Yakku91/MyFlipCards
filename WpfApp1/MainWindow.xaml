<Window x:Class="WpfApp1.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp1"
        mc:Ignorable="d"
        Title="MainWindow" Height="400" Width="300">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Label>Namen</Label>
        <ListBox Grid.Row="1" x:Name="listBox">
            
        </ListBox>
        <StackPanel Grid.Column="1" Grid.Row="1" Margin="5,0,0,0">
            <TextBox Name="textBox"/>
            <Button Margin="0, 5, 0, 0" Click="Button_Click" Name="myButton" AutomationProperties.AccessKey="" IsDefault="True">Name hinzufügen</Button>
        </StackPanel>
    </Grid>
</Window>
